# AI 自动化测试平台实现上下文

本文用于给研发、测试或 AI 助手快速说明当前 `dwt-testing` 的真实实现状态。早期 P0/P1 提示词已经落地为工程代码，本文件不再作为“待开发提示词”使用。

## 项目定位

`dwt-testing` 是一个可定制的 AI 辅助端到端测试平台，使用 `Node.js + TypeScript + Playwright + YAML DSL + Fastify + React + Electron` 构建。

平台以 YAML DSL 作为用例主入口，通过统一 runner 执行 Web、API 和 DB 只读断言，并通过 CLI、Web 控制台和桌面端查看运行状态、报告、日志、截图和 trace。

## 当前技术栈

- Monorepo：pnpm workspace。
- 后端：Fastify，入口在 `apps/server/src/index.ts`。
- 前端：React 19、Vite、Ant Design、Zustand、Axios、Monaco Editor。
- 桌面端：Electron，复用 Web 控制台和 Fastify API。
- Runner：Playwright、YAML、mysql2，只读 DB 查询。
- AI：OpenAI 兼容 Chat Completions 客户端，可接入 OpenAI、阿里云百炼或其他兼容服务。

## 核心模块

```text
apps/
  server/       Fastify API
  web/          React 控制台
  desktop/      Electron 桌面端
packages/
  shared/       公共类型、Schema、常量、变量解析、脱敏工具
  runner/       CLI、YAML 加载、预检、执行器、编排器、报告生成
  ai-generator/ AI 提示词构建、OpenAI 兼容客户端、截图分析、DSL 生成
cases/
  scenario/     用例流程 YAML
  location/     页面定位 YAML
  shared/       复用流程能力 YAML
  templates/    用例模板
```

## 已实现能力

- YAML DSL 加载与结构校验。
- `beforeActions`、`mainSteps`、`assertions`、`afterActions` 分阶段步骤展开。
- `cases/shared/**/*.yaml` 复用能力，通过 `use` / `with` 引用。
- 多 session 浏览器上下文，适配 user/admin 等多端流程。
- Web 步骤执行、截图、trace、运行事件和 HTML/JSON 报告。
- API 请求和断言，支持相对 URL、query、body、headers、业务码、响应字段断言、变量保存。
- API 步骤可复用同名浏览器 session 的 cookie。
- DB 只读查询和断言，必须显式开启 `DB_ENABLED=true`。
- 运行前预检，覆盖 DSL、环境变量、定位文件、API baseUrl、DB 开关、上传附件等。
- Web 控制台：Dashboard、用例列表、用例编辑、运行详情、报告查看、历史记录、设置页。
- AI 辅助：对话、用例草稿、资料导入、共享能力引用、截图失败分析。
- Electron 桌面端打包入口。

## 当前命令

```bash
pnpm dev
pnpm dwt list
pnpm dwt validate [caseId|file]
pnpm dwt preflight <caseId|file> [--env=local|dev|sit] [--no-env-file]
pnpm dwt plan <caseId|file> [--env=local|dev|sit] [--no-env-file]
pnpm dwt run <caseId|file> [--env=local|dev|sit] [--headless|--headed] [--no-env-file]
pnpm typecheck
pnpm test
pnpm ci:check
```

`prod` 是配置和构建可识别环境，但自动化运行守卫会阻止在 `prod/production` 或疑似生产域名上执行流程。

## 后端 API

主要接口分组：

- `/api/cases`：用例列表、详情、保存、删除、校验、预检、附件。
- `/api/test-runs`：创建运行、历史记录、运行详情、报告、日志。
- `/api/test-runs/:runId/events`：运行事件流。
- `/api/artifacts`：运行产物查询和清理。
- `/api/settings/env-files`：环境配置读取、保存、导入。
- `/api/app/context`：业务上下文读取、保存、解析、删除。
- `/api/ai/*`：AI 对话、用例草稿、资料生成、截图分析。
- `/api/db/health`：DB 只读连接健康检查。

## 安全边界

- 不在 YAML、文档、源码中写真实账号、密码、token、证书、DB 密码或 AI Key。
- `.env*` 和 CI Secret 承载敏感配置。
- DB 执行器只允许 `select/show/desc/describe/explain`，并拦截写操作和 DDL。
- `db_clean` 是预留步骤，当前执行器会拒绝。
- AI 生成结果必须经过人工审阅和 DSL 校验，尤其是资金、审批、状态流转、接口断言和 DB 断言。
- 报告、日志、截图、trace、上传资料可能包含业务信息，交付前需要脱敏。
