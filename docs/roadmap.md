# Roadmap

## P0

- YAML DSL 加载与 Zod 校验。
- user/admin 独立 browserContext。
- 页面流程执行、可视化辅助层、截图、trace、JSON/HTML 报告。
- Fastify API 和 React 控制台。

## P1

- API 请求和响应断言。
- DB 查询断言和安全 SQL 策略，目前已具备只读查询与健康检查基础能力。
- Playwright codegen 转 DSL 的完整定位抽取。
- 接入真实 AI Provider 生成 DSL 草稿。
- CI/CD 中执行无头模式回归。
