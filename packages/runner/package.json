{
  "name": "@ai-e2e/runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build:local": "tsc -p tsconfig.json --noEmit",
    "build:dev": "tsc -p tsconfig.json --noEmit",
    "build:sit": "tsc -p tsconfig.json --noEmit",
    "build:prod": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-e2e/shared": "workspace:*",
    "dayjs": "^1.11.20",
    "mysql2": "^3.22.3",
    "playwright": "^1.60.0",
    "yaml": "^2.9.0"
  }
}
