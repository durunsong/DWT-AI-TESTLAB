{
  "name": "@ai-e2e/ai-generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build:local": "tsc -p tsconfig.json --noEmit",
    "build:dev": "tsc -p tsconfig.json --noEmit",
    "build:sit": "tsc -p tsconfig.json --noEmit",
    "build:prod": "tsc -p tsconfig.json --noEmit",
    "test": "node ../../scripts/run-tests-if-present.mjs \"src/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-e2e/shared": "workspace:*",
    "yaml": "^2.9.0"
  }
}
